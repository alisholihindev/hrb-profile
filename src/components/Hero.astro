---
// Hero component with forest background matching the reference
---

<section id="home" class="relative min-h-screen overflow-hidden">
  <!-- Background Image -->
  <div class="absolute inset-0">
    <img 
      src="/images/forest-hero.png" 
      alt="Forest Background" 
      class="w-full h-full object-cover"
    />
    <!-- Dark overlay with explicit opacity -->
    <div class="absolute inset-0" style="background-color: rgba(0, 0, 0, 0.4);"></div>
  </div>
  
  <!-- Content -->
  <div class="relative z-10 container mx-auto px-6 py-20 flex items-center min-h-screen">
    <div class="max-w-4xl text-white">
      <h1 class="text-5xl md:text-7xl font-bold mb-6 leading-tight">
        Protecting Nature,<br>
        <span class="text-green-300">Sustaining the Future</span>
      </h1>
      <p class="text-xl md:text-2xl mb-8 text-gray-200 max-w-3xl leading-relaxed">
        Committed to sustainable forest management and environmental conservation 
        for generations to come.
      </p>
      <div class="flex flex-col sm:flex-row gap-4">
        <button class="bg-green-600 hover:bg-green-700 text-white px-8 py-4 rounded-lg font-semibold transition-all duration-300 shadow-lg">
          Learn About Us
        </button>
        <button class="border-2 border-white hover:bg-white hover:text-green-800 text-white px-8 py-4 rounded-lg font-semibold transition-all duration-300">
          Our Projects
        </button>
      </div>
    </div>
  </div>
</section>